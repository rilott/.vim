# .vim
Vim dotfiles

Includes:
  * Basic vimrc
  * Vim plugins using Vim 8 built-in plugin management (git submodules)
 
Installation:
  * Clone this repo to `~/`
  * `cd .vim`
  * `git submodule update --init`
